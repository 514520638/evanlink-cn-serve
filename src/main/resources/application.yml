server:
  port: 8080

spring:
  application:
    name: evanlink-cn-serve
  
  datasource:
    url: jdbc:mysql://62.234.72.18:3306/evanlink_cn?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 0.cptbtptp
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 500MB

logging:
  level:
    com.evanlink: debug
    org.hibernate.SQL: debug

app:
  upload:
    dir: uploads
    mirror:
      url: ${APP_UPLOAD_MIRROR_URL:}
      delete-url: ${APP_UPLOAD_MIRROR_DELETE_URL:}
      secret: ${APP_UPLOAD_MIRROR_SECRET:}
  admin:
    username: 14776866846
    password: 0.cptbtptp
